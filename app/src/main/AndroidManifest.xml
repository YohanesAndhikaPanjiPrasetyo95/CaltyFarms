<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andhikapanjiprasetyo.caltyfarm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_calty"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_calty_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BerandaActivity"/>
        <activity android:name=".KodeOTPActivity" />
        <activity android:name=".VerifikasiOTPActivity" />
        <activity android:name=".InputDataSapiActivity" />
        <activity android:name=".DaftarStatusSapiActivity" />
        <activity android:name=".LaporanMingguanActivity" />
    </application>

</manifest>